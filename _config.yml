# For more see: https://github.com/mojombo/jekyll/wiki/Permalinks
permalink: /:year-:month-:day/:title

exclude: ["README.md", "config.ru", "Gemfile", "Gemfile.lock", "Rakefile", "wercker.yml", "bin", "vendor"]
highlighter: pygments
paginate: 20

# maruku has a bug with the first line in the post starting with a colon
# https://github.com/mojombo/jekyll/issues/126
markdown: rdiscount
rdiscount:
  extensions: ['smart']

title: "GrinFit"
tagline: "Sprints, weights, ultimate."
author:
  name:  Grin
  email: grin@grin.io
timezone: "America/New_York"

production_url: http://fit.grin.io

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"
    values:
      layout: "post"

gems:
  - jekyll-sitemap
